<!DOCTYPE html>
<html>
  <head>
    <title>RecipeApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= javascript_include_tag 'toggle_switch' %>
  </head>

  <body>
  
  <nav class="navbar">      
  <% if user_signed_in? %> Logged in as <strong> <%= current_user.email %></strong> | <%= link_to "Home", users_path, class: "navbar-link" %> |
          <%= link_to "Edit profile", edit_user_registration_path, class: "navbar-link" %> |   
          <%= button_to "Sign out", destroy_user_session_path, method: :delete, class: "navbar-link"  %>      
          <% end %>
  </nav>

  <p class="notice"><%= notice %></p>
<p class="alert"><%= alert %></p>
<p class="error"><%= flash[:error] %></p>

    <%= yield %>
  </body>
</html>
